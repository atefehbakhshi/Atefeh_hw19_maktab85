import HeaderElement from "./HeaderElement";

const Remaining = () => {
  return (
    <HeaderElement bgColor={"#d4edda"}>
      <p>Remaining: &#163;{2000}</p>
    </HeaderElement>
  );
};

export default Remaining;
